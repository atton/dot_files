Pry.config.history.file = File.expand_path('~/.config/pry/history')
# Pry.history.instance_eval { @saver = ->(line) { save_to_file (line.force_encoding(STDIN.external_encoding))} }

# vim: set filetype=ruby:
